### Breaking

- `Ouroboros.Network.Socket` API is now parametrised by a monad `m`, except for
  the `connectToNodeSocket` which is a top level API using IO, except of
  `connectToNodeSocket` which is kept in `IO`
