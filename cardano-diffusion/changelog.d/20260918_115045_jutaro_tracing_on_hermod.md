### Breaking

- The `tracing` sublibrary now provides `LogFormatting` / `MetaTrace` instances
  for `hermod-tracing-api` (the successor of `trace-dispatcher`); it depends on
  `hermod-tracing-api:public ^>=1.1` instead of `trace-dispatcher`.
