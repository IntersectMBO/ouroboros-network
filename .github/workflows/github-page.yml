name: "Haddock documentation"

on:
  push:
  schedule:
    - cron:  '0 0 * * *'

jobs:
  haddocks:
    name: "Haddocks"

    runs-on: ${{ matrix.os }}

    defaults:
      run:
        shell: bash

    strategy:
      fail-fast: false
      matrix:
        ghc: ["8.10.2"]
        os: [ubuntu-latest]

    steps:
    - uses: actions/checkout@v1

    - name: deploy to gh-pages 🚀
      run: |
        git fetch origin gh-pages:gh-pages
        echo git push https://${{ github.actor }}:${{ github.token }}@github.com/${{ github.repository }}.git HEAD:gh-pages
